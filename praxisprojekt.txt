 # 数据量不足时的平滑处理：在某些区间内数据点较少的情况下，未加权的滑动窗口可能会给出不可靠的参考区间。加权滑动窗口能够将附近数据点的影响考虑进来，从而平滑结果，避免某些区间数据量不足导致的波动。
# 边界效应：在窗口边界处，未加权的滑动窗口可能会受到边界数据稀疏或异常值的影响较大。加权处理可以减少这种边界效应，将更多的注意力放在中心数据点上，而对边界数据点的权重降低。
# 连续性和稳定性：加权滑动窗口可以使得参考区间在随窗口移动时更加连续和平滑，减少剧烈波动，从而提供更稳定的参考区间。
# 灵活性：通过调整权重函数的形状和参数，可以灵活控制不同数据点对参考区间计算的影响，从而适应不同的应用场景和数据分布特点。




11.09已完成：
2. 增加了加权函数的灵活性，可以使用三种不同的分布函数
1. 确保尺度无关skalenunabhängig，加权函数的在分布的中心点标准化normalisieren为1
3. 创建了shiny应用程序


Aufgaben：
1. 修复错误，xct hochladen, a = 198  n.steps=339

函数参数：
	对于函数参数
		gaussian中接受x为在当前window中的点，mean自动作为当前点的平均值
		triangular中
			a是窗口最左侧
			b是所有点的中位数
			c是窗口最右侧
		trapezoidal
			a是窗口最左侧
			b是所有点的0.25分位
			c是所有点的0.75分位
			d是所有点的最右侧
用户决定要多大的数据
周三 周五






函数运行流程：
run
	w_sliding.reflim
		w_reflim
			w_lognorm
			w_iboxplot
			w_truncated_qqplot
			conf_int95

shiny应用不能展示第一张图片：问题是rstuido显示图片的窗口太小

关于分布函数以及分布函数的移动：
Gaussian：
	循环生成从covcomp的最小值到最大值之间的一个等距区间，分布函数最高点均匀移动，因此不一定会有点的权值为1
else：
	如果窗口够大，窗口的左右端点各自向右+1
	如果窗口不够大，又端点向右+1直到够大为止
	随后，分布函数在窗口中取第n个百分位点作为分布函数端点进行绘制


24.09 Fragen:
1.
在运行gaussian分布的时候，r提示Warning in regularize.values(x, y, ties, missing(ties), na.rm = na.rm) :collapsing to unique 'x' values表示在计算过程中遇到了重复的或相同的 x 值
当 x 中有重复值时，这个函数会尝试将这些重复值处理为唯一值，以避免在绘图或计算统计量时出现问题

在运行truncated_gaussian的时候，尝试使用any(duplicated(xxx))来输出发现在此分布下，Fenster中的x任然存在重复，那么为什么在此分布下不提示warning而只在gaussian中提示
	
2.
对于分布，什么样子的分布应该是需要的。对于有fenster的分布，目前分布函数每个点都依赖于fenster中的点。而对于gaussian，分布函数随机移动 ok

3. todo
为什么gaussian中没有权值为1的点：测试中似乎是有权值为1的点的，但是图中没有红色的点




verteilungsfunkion soll gleich mit gleich Parameter

Gewichtung  rot - blau

10uhr mittwoch
任务：
1. 修改除了gaussian的分布函数，保证分布函数形状始终一致
完成了对中间参数的固定，但是还要考虑窗口大小的问题
 查看 truncated gaussian参数修改或者删掉此分布

2. 为shiny应用第一张图添加颜色		ok
3. 解决warning


问题29.09：
readme中的license要怎么写